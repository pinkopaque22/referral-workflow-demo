<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Referral Workflow" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">
    <%= favicon_link_tag "favicon.ico" %>

    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <%# Header / Navigation %>
    <header style="padding: 1rem; border-bottom: 1px solid #ccc; display: flex; justify-content: space-between; align-items: center;">
      <h1>Referral Workflow</h1>
      <nav>
        <% if current_user %>
          <span>Logged in as <%= current_user.name %> (<%= current_user.role %>)</span>
          <%= button_to "Logout", logout_path, method: :delete, class: "btn btn-sm btn-outline" %>
        <% else %>
          <%= link_to "Login", login_path, class: "btn btn-sm btn-primary" %>
        <% end %>
      </nav>
    </header>

    <main style="padding: 1rem;">
      <%# Flash messages %>
      <% if flash[:notice] %>
        <div class="flash-notice" style="color: green; margin-bottom: 1rem;">
          <%= flash[:notice] %>
        </div>
      <% end %>
      <% if flash[:alert] %>
        <div class="flash-alert" style="color: red; margin-bottom: 1rem;">
          <%= flash[:alert] %>
        </div>
      <% end %>

      <%= yield %>
    </main>
  </body>
</html>
