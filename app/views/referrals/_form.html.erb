<%= form_with(model: referral) do |form| %>
  <% if referral.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(referral.errors.count, "error") %> prohibited this referral from being saved:</h2>

      <ul>
        <% referral.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label : student_name, style: "display: block" %>
    <%= form.text_field : student_name %>
  </div>

  <div>
    <%= form.label : student_info, style: "display: block" %>
    <%= form.textarea : student_info %>
  </div>

  <div>
    <%= form.label : status, style: "display: block" %>
    <%= form.text_field : status %>
  </div>

  <div>
    <%= form.label : teacher_id, style: "display: block" %>
    <%= form.text_field : teacher_id %>
  </div>

  <div>
    <%= form.label : counselor_id, style: "display: block" %>
    <%= form.text_field : counselor_id %>
  </div>

  <div>
    <%= form.label : admin_id, style: "display: block" %>
    <%= form.text_field : admin_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
